[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn legend_server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/overview"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
